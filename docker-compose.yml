version: "3.8"

services:
  server:
    image: my_custom_api:latest
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_KEY=${DB_KEY:-S3cr3tIsThisYouKnowna}
      - DB_URL=${DB_URL:-mongodb+srv://harsh:harsh123@cluster0.poozi.mongodb.net/course_selling_app}
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - server
    restart: unless-stopped
